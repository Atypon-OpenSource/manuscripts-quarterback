{
  "name": "@manuscripts/examples-track-e2e",
  "version": "0.0.1",
  "scripts": {
    "format": "prettier --write \"*.+(js|json|yml|yaml|ts|md|graphql|mdx)\" cypress/",
    "lint": "eslint --cache --ext .js,.ts, ./cypress",
    "lint:fix": "eslint --fix --ext .js,.ts, ./cypress",
    "start": "cypress open",
    "test": "cypress run --browser chrome",
    "cy": "cypress"
  },
  "devDependencies": {
    "@manuscripts/track-changes-plugin": "workspace:*",
    "@testing-library/cypress": "^8.0.1",
    "cypress": "^8.7.0",
    "typescript": "^4.4.3"
  }
}
